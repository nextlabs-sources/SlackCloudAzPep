import {SlackSettings, SlackUser, SlackChannel} from '../models/data_models';
import {log} from './logger';

class SlackUtil {

    constructor() {
        
    }

}

export const slackUtil = new SlackUtil;